/** Matches/reconciles normalized transactions against Aval's existing financial records; classifies with an explicit confidence level, never a guess. Empty this round. */
package com.aval.openfinance.reconciliation;
