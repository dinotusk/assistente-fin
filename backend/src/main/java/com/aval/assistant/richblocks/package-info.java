/**
 * Rich Financial Responses (roadmap Fase 6): typed result payloads (e.g.
 * {@code type: "month_comparison", resultId: "..."}) the LLM references by
 * semantics only — the frontend controls presentation, the model never
 * generates UI. Empty this round.
 */
package com.aval.assistant.richblocks;
