/**
 * Reads {@code finance_months}/{@code profile_budgets} — the source data {@code
 * budgetForView} (see {@link com.aval.finance.summary.FinancialCalculator#budgetFor}) is
 * computed from. Read-only this round (P2-FINANCIAL-DOMAIN).
 */
package com.aval.finance.budgets;
