/**
 * Reads {@code expenses} rows — mirrors calc.ts's {@code Expense}/{@code expensesForView}.
 * Read-only this round (P2-FINANCIAL-DOMAIN): no write path yet. Named {@link
 * com.aval.finance.expenses.FinancialEntry}, not {@code Expense} — the table holds both expense
 * and income rows.
 */
package com.aval.finance.expenses;
