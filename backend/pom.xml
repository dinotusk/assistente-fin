<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!--
    P1-SPRING-BOOT-FOUNDATION.

    Version choice (see docs/architecture/ADR-002-spring-boot-supabase.md):
    Spring Boot 3.5.x reached end-of-life on 2026-06-30, so for a brand-new
    foundation Spring Boot 4.x is the only responsibly supportable line, not
    a stylistic preference. Pinned to the latest patch, 4.1.1 (GA
    2026-08-20, confirmed on Maven Central).
  -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>com.aval</groupId>
  <artifactId>aval-backend</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>aval-backend</name>
  <description>Aval V1 — Spring Boot foundation (API, domain, integrations, platform)</description>
  <packaging>jar</packaging>

  <properties>
    <!-- Java 25 LTS (current LTS as of 2026-08; Spring Boot 4 requires 17+). -->
    <java.version>25</java.version>
    <maven.compiler.release>25</maven.compiler.release>

    <!-- Not managed by spring-boot-dependencies — pinned explicitly. -->
    <springdoc-openapi.version>3.0.3</springdoc-openapi.version>
    <testcontainers.version>2.0.3</testcontainers.version>
  </properties>

  <dependencies>
    <!-- Core web API -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- Security: Supabase-issued JWT validated as an OAuth2 Resource Server. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
    </dependency>

    <!--
      Plain JDBC, not JPA: there is no domain entity to map yet (this round
      intentionally ships no financial endpoints — see ADR-005). Pulling in
      Hibernate/JPA with zero @Entity classes would be unused weight; a
      DataSource + JdbcTemplate is enough to prove real connectivity. JPA is
      revisited in P2-FINANCIAL-DOMAIN once there is something to map.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Schema migrations (configured, not exercised — see ADR/README: existing schema untouched this round). -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>

    <!-- Observability -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
      <scope>runtime</scope>
    </dependency>
    <!-- OpenTelemetry bridge: wires Micrometer Tracing to OTLP once an exporter endpoint is configured; inert (no-op) otherwise. -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-tracing-bridge-otel</artifactId>
    </dependency>
    <dependency>
      <groupId>io.opentelemetry</groupId>
      <artifactId>opentelemetry-exporter-otlp</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- API documentation -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc-openapi.version}</version>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-postgresql</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <dependencyManagement>
    <dependencies>
      <!--
        Testcontainers 2.x is not (yet, as of this scaffold) covered by
        spring-boot-dependencies' managed versions, and its 2.0 line renamed
        every module artifact (the old "postgresql" module is now
        "testcontainers-postgresql") — pin explicitly via its own BOM rather
        than guess at compatible individual versions.
      -->
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <finalName>aval-backend</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
